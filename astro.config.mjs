// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    base: '/amormio/', // 👈 nombre de tu repositorio
    outDir: './dist', // esto ya debe estar por defecto
});
